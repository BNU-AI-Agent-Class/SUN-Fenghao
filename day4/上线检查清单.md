# 上线检查清单

## 1. 🔑 密钥安全（一票否决）

- [x] 前端源码无真实 `sk-...` / Google API key。
- [x] 模型 key 只在后端环境配置。
- [x] `.env` 在 `.gitignore`，未提交。
- [x] 提交仓库 `git log -p` 未检出 key 形状。

## 2. ✅ 可用性

- [x] [公网 URL](https://dream.qimingbot.com) 能打开。
- [x] 演示账号可登录并完成一轮线上对话。
- [x] Playwright 在 `390×844` 手机视口验证，输入区和紧急求助按钮均可操作。
- [x] 提供普通演示账号，非开发者也可复现。

## 3. 🛟 内容合规与安全（一票否决）

- [x] 空会话明示“我是 AI，不能替代专业或紧急帮助”。
- [x] 明示对话用于陪伴并保存到用户账户。
- [x] 常驻“紧急求助”按钮可见，点击后显示全国 12356、紧急 110/120。
- [x] 8 类危机输入线上复测全部进入现实转介，不提供方法。

## 4. 🧯 优雅降级

- [x] 网络/模型失败不白屏、不显示堆栈。
- [x] 错误时向消息流写入本地兜底，而不只是短暂 toast。
- [x] 本地兜底含 12356、110、120，不依赖后端。

## 5. 📄 README 三要素

- [x] 写明帮助谁、什么场景、解决什么问题。
- [x] 提供网址、演示账号和示例。
- [x] 写明小组成员、分工和课程 Day4。

## 验收时间

2026-07-15：`/api/health` 返回 `{"status":"ok"}`；13 条线上评估均 HTTP 200；危机复测 8/8 PASS。
