# 启明课程作业补全计划

**目标：** 保留已完成的 Day1，以真实的启明开发过程为证据，完成 Day2、Day3、Day4 和 15 分钟汇报材料。

**总原则：** 默认不修改启明源码；研究旧数据只用于需求证据，当前版本评估只使用实际调用结果；危机测试若失败，先报告再做最小修复。

## 1. Day2：mini Claude Code 新工具

- 在 `hw2/` 复制独立的 `demo_project/`，不改老师原件。
- 以 `c1_toolbox.py` 为基础实现 `search` 工具：函数、工具注册、系统说明三处对齐。
- 加入未知工具与有限重试，避免模型幻觉工具名时无限循环。
- 用修改后的 agent 复现并修复 `analyze.py` 无标签时崩溃的问题。
- 保存运行记录、修复 diff、测试和口语化说明。

## 2. Day3：DEFINE + Eval

- 按老师模板重写 `day3/T1-T8`，删除“待填”和已经过时的未来设想。
- T1-T3：从旧版启明的 177 名参与者脱敏材料提取聚合证据，并明确数据版本。
- T4-T5：只保留三个核心产品能力，将完整工程能力放入实现栏，不让 PRD 变成功能堆砌。
- T6-T7：对当前线上版本运行正常、异常、边界和危机测试，保存原始输出；四维评分逐项引用证据。
- T8：使用真实开发故障和修复记录，写至少两轮“问题、根因、改动、复测”。
- 生成 2-3 页 Capstone 和证据索引，标注 LLM 评委偏差与人工复核责任。

## 3. Day4：Build + Ship

- 用现有 Vue/FastAPI/服务器部署映射老师的 B1-B4、S1-S4，不为迎合示例改成 Railway。
- 填技术架构卡、API 契约、部署选型、密钥清单、上线检查和结项技术文档。
- 验证公网首页、健康接口、登录、正常对话、危机转介、失败降级和移动端布局。
- 扫描前端、提交目录和 Git 历史中的密钥；只记录变量名，不复制真实值。
- 创建课程演示账号；提交文档仅给普通账号，不暴露管理员凭据。
- 提供 30-60 秒手机录屏的简短分镜，视频由孙凤豪最终录制。

## 4. PPT

- 15 分钟，建议 12-14 页。
- 主线：一个具体用户 → 五条证据 → 缺失环节 → 三层需求 → 三个核心功能 → 工作流 → 安全 → 实测 → 迭代 → 上线。
- 旧 PPT 只复用有效架构图和产品截图，不沿用“都市人群”与科幻故事开场。
- 生成可编辑 `.pptx`、讲稿和图源；完成文本、溢出、对齐与移动端截图 QA。

## 5. 提交与验收

- Day1 文件不覆盖。
- 本地运行 Day2 测试与启明现有测试；记录命令和结果。
- 汇总 Day2/Day3/Day4/PPT 到 `SUN-Fenghao`，更新根 README 导航。
- 最后做敏感信息扫描、检查 Git diff，提交并推送 `main`。

